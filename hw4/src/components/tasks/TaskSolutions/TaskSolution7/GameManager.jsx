import { getRandomNumber } from '@/utils'

function GameManager() {
  const randomNumber = getRandomNumber(0, 9)

  function getNumbers() {}
  return (
    <div>
      <h2 className="p-1 border rounded-xl">Game "Guess the Number"</h2>
			
    </div>
  )
}

export default GameManager
